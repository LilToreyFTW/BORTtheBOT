{
  "name": "server",
  "main": "src/index.ts",
  "type": "module",
  "scripts": {
    "build": "tsdown",
    "check-types": "tsc -b",
    "build:exe": "bun run build && bun build --compile --minify --sourcemap ./src/launcher-dist.ts --outfile ./BORTtheBOT-Server.exe --target bun",
    "build:exe:unix": "bun run build && bun build --compile --minify --sourcemap ./src/launcher-dist.ts --outfile ./BORTtheBOT-Server --target bun",
    "bundle": "bun build ./src/launcher.ts --outdir ./bundled --target bun --minify",
    "compile": "npm run build && bun build --compile --minify --sourcemap ./src/launcher-dist.ts --outfile BORTtheBOT-Server.exe",
    "compile:unix": "npm run build && bun build --compile --minify --sourcemap ./src/launcher-dist.ts --outfile BORTtheBOT-Server",
    "dev": "bun run --hot src/index.ts",
    "start": "bun run dist/index.js",
    "launch": "bun run src/launcher.ts"
  },
  "dependencies": {
    "hono": "catalog:",
    "@trpc/server": "catalog:",
    "@hono/trpc-server": "^0.4.0",
    "better-auth": "catalog:",
    "dotenv": "catalog:",
    "zod": "catalog:",
    "@project/api": "workspace:*",
    "@project/auth": "workspace:*",
    "@project/db": "workspace:*"
  },
  "devDependencies": {
    "typescript": "catalog:",
    "@types/bun": "^1.2.6",
    "tsdown": "catalog:",
    "pkg": "^5.8.1"
  },
  "pkg": {
    "scripts": [
      "dist/index.js"
    ],
    "assets": [
      "node_modules/**/*",
      "../../packages/**/*"
    ],
    "targets": [
      "node18-win-x64"
    ],
    "outputPath": "BORTtheBOT-Server.exe"
  }
}
